@model IEnumerable<web_lab1_fandom.Models.Casts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<!--
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstAppereance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastAppereance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Series)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Actor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Character)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstAppereance)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastAppereance)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Series.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Actor.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Character.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>-->




<style>
    body {
        font-family: Arial, Helvetica, sans-serif;
    }

    .flip-card {
        background-color: slategrey;
        width: 320px;
        height: 500px;
        perspective: 1000px;
    }

    .flip-card-inner {
        position: relative;
        width: 100%;
        height: 100%;
        text-align: center;
        transition: transform 0.6s;
        transform-style: preserve-3d;
        box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
    }

    .flip-card:hover .flip-card-inner {
        transform: rotateY(180deg);
    }

    .flip-card-front, .flip-card-back {
        position: absolute;
        width: 100%;
        height: 100%;
        -webkit-backface-visibility: hidden;
        backface-visibility: hidden;
    }

    .flip-card-front {
        color: black;
        justify-content: center;
    }

    .flip-card-back {
        color: black;
        transform: rotateY(180deg);
        justify-content: center;
    }

    .flip-img {
        height: 400px;
        width: 320px;
        object-fit: contain;
    }
</style>

<div class="row mb-4 wow fadeIn justify-content-center">
@foreach (var item in Model)
{
    <div class="flip-card card mb-3" style="display: inline-block; margin:10px">
        <div class="flip-card-inner">
            <div class="flip-card-front">
                <img class="card-img-top flip-img" src="@item.Character.Photo" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@item.Character.Name</h5>
                </div>
            </div>
            <div class="flip-card-back">
                <img class="card-img-top" src="@item.Actor.Photo" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@item.Actor.Name</h5>
                    <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-info">Info</a>
                    <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-secondary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-danger">Delete</a>
                </div>
            </div>
        </div>
    </div>
}

</div>


